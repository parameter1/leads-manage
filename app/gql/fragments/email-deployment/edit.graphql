fragment EmailDeploymentEditFragment on EmailDeployment {
  id
  name
  subject
  preheader
  category {
    id
    name
    fullName
  }
  sends(pagination: { first :1 }, sort: { field: "sentDate", order: 1 }) {
    totalCount
  }
  externalSource {
    identifier
    namespace
  }
}
