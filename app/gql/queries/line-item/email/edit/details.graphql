query EditEmailLineItemDetails($input: ModelIdInput!) {
  emailLineItem(input: $input) {
    id
    name
    requiredLeads
    totalValue
    notes
    displayOnDashboard
    range {
      start
      end
    }
  }
}
