query searchCustomers($pagination: PaginationInput, $search: CustomerSearchInput!) {
  searchCustomers(pagination: $pagination, search: $search) {
    totalCount
    edges {
      node {
        id
        name
      }
      cursor
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
