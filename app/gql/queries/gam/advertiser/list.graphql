query ListGAMAdvertisers($input: GAMAdvertisersQueryInput!) {
  GAMAdvertisers(input: $input) {
    totalCount
    nodes {
      id
      name
      customer {
        id
        name
      }
    }
    pageInfo {
      hasNextPage
      nextOffset
    }
  }
}
