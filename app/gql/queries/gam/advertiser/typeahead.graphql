query GAMAdvertiserTypeahead($phrase: String!) {
  GAMAdvertisers(input: {
    search: { field: NAME, phrase: $phrase }
    sort: { field: NAME, order: ASC }
    limit: 50
  }) {
    nodes {
      id
      name
    }
  }
}
