{{#route-header}}Customers{{/route-header}}

{{#paginate-models
  models=model
  routeName='customer.index'
  totalCount=totalCount
  hasNextPage=hasNextPage
  endCursor=endCursor
  first=first
  after=after
  ascending=ascending
  sortBy=sortBy
  sortOptions=sortOptions
  as |model|
}}
  {{partial 'customer/list'}}
{{/paginate-models}}

{{model-create-button click=(route-action "transitionTo" "customer.create")}}
