<div class="row">
  <div class="col">
    <h3 class="mb-0">
      Users
    </h3>
  </div>
</div>
<hr>

{{#paginate-models
  models=model
  routeName='user.index'
  totalCount=totalCount
  hasNextPage=hasNextPage
  endCursor=endCursor
  first=first
  after=after
  ascending=ascending
  sortBy=sortBy
  sortOptions=sortOptions
  as |model|
}}
  {{partial 'user/list'}}
{{/paginate-models}}

<button class="btn btn-success create float-right fixed-bottom" {{action (route-action "transitionTo" "user.create")}}>
  {{entypo-icon "pencil"}} Create
</button>
