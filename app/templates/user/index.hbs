{{#route-header}}Users{{/route-header}}

{{#paginate-models
  models=model
  routeName='user.index'
  totalCount=totalCount
  hasNextPage=hasNextPage
  endCursor=endCursor
  first=first
  after=after
  ascending=ascending
  sortBy=sortBy
  sortOptions=sortOptions
  as |model|
}}
  {{partial 'user/list'}}
{{/paginate-models}}

{{model-create-button click=(route-action "transitionTo" "user.create")}}
