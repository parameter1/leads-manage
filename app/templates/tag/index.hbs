{{#route-header}}Tags{{/route-header}}

{{#paginate-models
  models=model
  routeName='tag.index'
  totalCount=totalCount
  hasNextPage=hasNextPage
  endCursor=endCursor
  first=first
  after=after
  ascending=ascending
  sortBy=sortBy
  sortOptions=sortOptions
  as |model|
}}
  {{partial 'tag/list'}}
{{/paginate-models}}

{{model-create-button click=(route-action "transitionTo" "tag.create")}}
